auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# turbo 的平台二进制（@turbo/darwin-arm64 等）在部分国内镜像上缺失，
# 会导致可选依赖被静默跳过、turbo 启动时报 "did not find any binaries"。
# 只把该 scope 指回官方源，其余包仍走用户全局配置的镜像。
@turbo:registry=https://registry.npmjs.org/
